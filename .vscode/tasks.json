{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "MQTT server",
            "type": "shell",
            "command": "mosquitto.exe",
            "args": [
                "-v",
                "-c",
                "${workspaceFolder}\\tools\\mosquitto.conf"
            ],
            "options": {
                "cwd": "C:\\Program Files\\mosquitto"
            }
        }
    ]
}